#include <ui_events.h>

// Qt transformation: In Qt, custom events are defined as QEvent::Type constants
// These replace wxDEFINE_EVENT macros from wxWidgets
// The events are registered at compile time and used through Qt's event system

// Qt transformation completion marker: wxWidgets events converted to Qt QEvent::Type system