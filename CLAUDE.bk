# KiCad PCB依赖分析与最小集合提取指导

## 🎯 当前任务目标
**使用scripts目录中的新方法收集最小编译集合，基于符号依赖闭包分析找到精确的最小可编译集合**

使用改进的三阶段依赖分析工具，精确计算出最小依赖集合。

## 📊 策略概述
- **分析方法**: 使用scripts目录中的三阶段分析流程
- **分析工具**: `scripts/20_build_tu_index.py` → `scripts/30_resolve_minset.py`
- **种子文件**: KiCad PCB IO核心文件 (`pcb_io_kicad_sexpr_parser.cpp` 等)
- **输出结果**: 最小可编译源文件集合 + 符号依赖分析

## 🛠️ 第三方依赖管理策略

### KiCad的第三方依赖有两种类型：

#### 1. **thirdparty/目录依赖** (内置第三方库)
- **位置**: `thirdparty/` 目录中
- **特点**: KiCad项目内置，与源码一起分发
- **示例**: clipper2, magic_enum, fmt, nlohmann_json等
- **处理方式**: 从KiCad源码复制到qt_pcb_project/thirdparty/目录

#### 2. **vcpkg依赖** (外部包管理器)
- **位置**: 通过vcpkg包管理器安装
- **特点**: 大型第三方库，通过包管理器管理版本
- **示例**: wxWidgets, boost, cairo, glew, opencascade等
- **处理方式**: 告知用户需要安装，用户会负责安装

### 🎯 依赖分析目标
**通过精确的依赖分析，一次性找到完整的最小依赖集合，避免试错编译的低效过程**

## 🛠️ 使用scripts目录中的三阶段分析流程

### 工具说明
使用scripts目录中的改进版三阶段分析流程：
1. `scripts/20_build_tu_index.py` - 构建翻译单元符号索引
2. `scripts/30_resolve_minset.py` - 基于符号依赖解析最小集合

### 工具准备
- **Python 3.10+**: 确保Python环境可用
- **符号提取工具**: dumpbin.exe (MSVC) 或 objdump (GCC)
- **编译环境**: 确保KiCad已编译完成

### 种子文件配置
种子文件在 `scripts/seeds.txt` 中配置：
```
pcb_io_kicad_sexpr_parser.cpp
```

### 三阶段分析流程
1. **阶段1 - 符号索引构建**: `scripts/20_build_tu_index.py`
   - 扫描编译产物(obj文件)
   - 提取每个翻译单元的已定义和未定义符号
   - 构建符号到源文件的映射关系

2. **阶段2 - 依赖闭包解析**: `scripts/30_resolve_minset.py`  
   - 从种子文件开始迭代
   - 解析内部符号依赖(排除外部库符号)
   - 计算符号传递闭包直至收敛

3. **阶段3 - 结果输出**:
   - `build/minset_sources.json` - 最小源文件集合
   - `build/unresolved_symbols.json` - 未解析符号(主要是外部库符号)

## 📋 执行检查清单

### 工具环境验证
- [ ] Python 3.10+环境就绪
- [ ] dumpbin.exe或objdump工具可用
- [ ] KiCad项目已成功编译，obj文件存在
- [ ] scripts目录中的分析脚本存在

### 执行验证  
- [ ] 种子文件在seeds.txt中配置完成
- [ ] 符号索引构建成功
- [ ] 依赖闭包计算收敛
- [ ] 输出文件完整生成

### 结果质量验证
- [ ] 最小集合包含所有种子文件
- [ ] 源文件数量在合理范围
- [ ] 未解析符号主要为外部库符号
- [ ] 外部依赖符号被正确过滤

## 🚀 执行命令序列

### 三阶段执行流程
```powershell
# 在KiCad源码根目录执行

# 阶段1: 构建符号索引
python scripts/20_build_tu_index.py

# 阶段2: 解析最小集合
python scripts/30_resolve_minset.py

# 检查结果
type build\minset_sources.json
type build\unresolved_symbols.json
```

## 📊 预期输出规模
基于PCB IO核心文件的符号依赖分析预估：
- **最小源文件集合**: 根据符号闭包计算确定
- **未解析符号**: 主要为外部库符号(boost, wx, cairo等)
- **外部依赖**: thirdparty和vcpkg依赖通过符号前缀识别
- **处理时间**: 取决于编译产物规模和符号复杂度

## ⚠️ 重要提醒

1. **符号分析** - 基于编译产物的符号依赖，比文本分析更精确
2. **外部库过滤** - 通过EXTERNAL_HINTS正确识别外部库符号
3. **种子配置** - seeds.txt中配置PCB IO相关核心文件
4. **收敛验证** - 确保符号闭包迭代收敛，无循环依赖

## 🎯 成功标准

当三阶段分析完成后，应该获得：
- **精确的最小依赖集合** - 通过符号依赖闭包计算得出
- **完整的符号映射** - build/tu_index.json包含详细符号信息
- **清晰的外部依赖识别** - 未解析符号主要为外部库
- **可执行的复制清单** - minset_sources.json可直接用于文件复制

此时可以基于符号级精确分析结果，复制最小必要文件到qt_pcb_project目录，开始Qt化改造工作。